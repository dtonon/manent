output: dist/

releases:
  - name: linux
    jobs:
      - name: release-linux-appimage
        package:
          platform: linux
          target: appimage
          options:
            icon: assets/icon.png
